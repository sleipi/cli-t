# --version shows version string
./clitest --version
EXIT 0
[asserts]
stdout matches /clitest version \S+/

# -V short flag also shows version
./clitest -V
EXIT 0
[asserts]
stdout matches /clitest version \S+/
