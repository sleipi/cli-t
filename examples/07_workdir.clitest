# === Working Directory ===
# The @workdir directive sets the working directory for commands.
# Relative paths resolve relative to this .clitest file's location.

# Entry-level @workdir
@workdir ./workdir_demo
cat file.txt
EXIT 0
example file
